<%= simple_form_for([@note.article, @note]) do |f| %>
  
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :content %>
    <%= f.input :selection %>
    <%= f.input :block %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
